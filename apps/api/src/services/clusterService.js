// src/services/clusterService.js
export async function updateUserCluster(user_id, embedding) {
  // 지금은 아무것도 안 함 (더미)
  return true;
}

export async function recalcClusters() {
  // 지금은 아무것도 안 함
  return true;
}
